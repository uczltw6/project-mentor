# Greeting sample

This intentionally minimal project contains one Python module and no environment setup. Run the behavior with:

```text
python -m unittest discover -s tests
```

The expected greeting for `Ada` is `Hello, Ada!`.
